import React from 'react'

var Header = React.createClass({
    render() {
        return (
            <div className='header'>Business model canvas with React</div>
        )
    }
})

export default Header
